import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "StudyOS - Your Ultimate Study Companion",
  description: "StudyOS is a comprehensive study companion designed to enhance your learning experience. With features like Pomodoro timers, task management, and progress tracking, StudyOS helps you stay focused and organized. Whether you're a student or a lifelong learner, StudyOS provides the tools you need to succeed in your studies.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
